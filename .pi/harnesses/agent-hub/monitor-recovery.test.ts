import test from 'node:test';import assert from 'node:assert/strict';import {stableMonitorHubId,monitorReconcileEvidence} from './monitor-recovery.ts';
test('stable hub identity survives PID changes and fails closed without Herdr IDs',()=>{const a=stableMonitorHubId({profileId:'p',checkout:'/x',workspaceId:'w',paneId:'q'}),b=stableMonitorHubId({profileId:'p',checkout:'/x',workspaceId:'w',paneId:'q'});assert.equal(a,b);assert.throws(()=>stableMonitorHubId({profileId:'p',checkout:'/x'}),/Herdr/);});
test('Herdr errors are transient rather than owner gone',async()=>assert.deepEqual(await monitorReconcileEvidence({owner:false,socket:false,session:false,paneId:'p',workspaceId:'w',herdr:{pane:{get:async()=>{throw Error()}},workspace:{get:async()=>null}}}),{owner:false,socket:false,session:false,herdr:true,transient:true}));
