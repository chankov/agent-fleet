---
description: Inspect or safely install the fail-closed Hermes watchdog
argument-hint: "<status|install|update|uninstall> [--profile NAME] [--force] [--dry-run]"
---

Before any non-`status` write, ask the user to confirm the exact profile and whether `--force` is intended. Run only the packaged deterministic controller. It never starts, stops, restarts, or kills Hermes or a watcher; it preserves watchdog config and journals. Gate O has no genuine reviewed live proof of origin routing, so do not initiate a watch or claim delivery, out-of-turn updates, steering, or surgical recovery.
