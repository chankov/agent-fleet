---
description: Inspect or safely install the fail-closed Hermes watchdog
argument-hint: "<status|install|update|uninstall> [--profile NAME] [--force] [--dry-run]"
---

Ask for confirmation before install, update, uninstall, or force. Use only the deterministic Agent Fleet watchdog controller with the supplied separately quoted arguments. `status` is read-only; `--dry-run` writes nothing. Never start/restart a gateway or watcher, change tools, send a message, initiate a watch, or infer Gate O/origin routing. The packaged skill remains journal-only/dormant until independently reviewed live Gate O evidence exists.
