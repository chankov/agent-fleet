---
"@chankov/agent-fleet": major
---

Namespace every Agent Fleet Pi slash command under `/af-*`, including lifecycle prompt templates, harness controls, utility-extension commands, and installer entrypoints. Pi bootstrap and guided setup migrate owned legacy prompt targets while preserving modified or unowned files.
