---
"@chankov/agent-fleet": patch
---

Document the terminal-specific macOS requirement for Pi Alt shortcuts: terminals must send Option as Meta, and Zed users should enable `terminal.option_as_meta`. This preserves the existing cross-platform shortcuts and avoids presenting a Zed configuration fix as an OS-wide keybinding change.
