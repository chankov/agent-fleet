---
"@chankov/agent-fleet": patch
---

Refresh the root, MCP-extension, and harness runtime lockfiles to patched Pi, HTTP, schema-validation, and YAML transitive dependencies. Add an offline lock-floor regression so vulnerable resolutions cannot silently return.
