---
"@chankov/agent-fleet": patch
---

Accept and preserve canonical nested voice STT configuration, distinguish installer aliases from runtime provider types, and require prepared provider settings instead of guessing them.
