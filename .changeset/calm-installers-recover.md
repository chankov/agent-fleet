---
"@chankov/agent-fleet": patch
---

Make setup prompts explicit and add a platform-compatible Full + all-features snapshot. Harden lifecycle dry-runs, public JSON, STT preservation, single-writer locking, path validation, durable transaction recovery, runtime retries, readiness reporting, and packed installer coverage.
