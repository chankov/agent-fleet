---
"@chankov/agent-fleet": patch
---

Add opt-in System 1 observations for native drift watchdog checks, safe trace/UI reporting, bounded recovery after an unavailable LLM judge, and an explicitly opted-in experimental active shortcut for advisory scope only (Jev 0.95/0.05). Active is not G2-validated; the default remains off and terminal rules retain the LLM judge.
