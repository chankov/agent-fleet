---
"@chankov/agent-fleet": patch
---

Add opt-in shadow System 1 observations for native drift watchdog checks, safe trace/UI reporting, a closed-gate active shortcut implementation, and bounded recovery after an unavailable LLM judge. Active remains unapproved and off by default.
