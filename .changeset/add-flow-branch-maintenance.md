---
"@chankov/agent-fleet": minor
---

Add numbered `just flow cleanup` and `just flow merge` branch maintenance, persisted source/result metadata, safe Worktrunk removal, and accepted-run squash merging back to the originating branch.
