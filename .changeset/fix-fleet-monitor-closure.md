---
"@chankov/agent-fleet": patch
---

Include `scripts/lib/monitor-env.ts` in the guided workspace setup companion manifest so copy and symlink installs can load the unified `just fleet` entrypoint.
